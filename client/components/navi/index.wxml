<view class="container">
  <image bind:tap="handleTapNext" class="icon" src="{{ latest ? disLeftSrc : leftSrc }}"></image>
  <text class="title">{{ title }}</text>
  <image bind:tap="handleTapPrev" class="icon" src="{{ first ? disRightSrc : rightSrc }}"></image>
</view>
